{
  "name": "casamento-thalia-gian",
  "private": true,
  "version": "1.0.0",
  "description": "Site de casamento Gian & Thalia — frontend (React) + backend (Cloudflare Worker)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "wrangler dev",
    "build": "npm run build -w frontend",
    "deploy": "npm run build -w frontend && wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply casamento-db --local",
    "migrate:remote": "wrangler d1 migrations apply casamento-db --remote",
    "create-admin": "node backend/scripts/create-admin.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  },
  "comment_overrides": "Usa o Rollup em WebAssembly (sem binário nativo por plataforma) para evitar o bug npm/cli#4828 no build Linux do Cloudflare.",
  "overrides": {
    "rollup": "npm:@rollup/wasm-node@^4.62.2"
  }
}
