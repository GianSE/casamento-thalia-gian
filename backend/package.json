{
  "name": "casamento-thalia-gian-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "API do casamento Gian & Thalia — Hono no Cloudflare Workers (D1 + Cloudinary)",
  "//": "Os comandos do wrangler (dev, deploy, migrações) vivem no package.json da raiz, junto do wrangler.toml — o deploy publica o Worker e o frontend juntos.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
