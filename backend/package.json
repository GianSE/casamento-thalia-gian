{
  "name": "casamento-thalia-gian-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "API do casamento Gian & Thalia — Hono no Cloudflare Workers (D1 + Cloudinary)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply casamento-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply casamento-db --remote"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
